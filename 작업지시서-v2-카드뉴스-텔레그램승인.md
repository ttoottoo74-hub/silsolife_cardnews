# 작업 지시서 v2 — 카드뉴스를 텔레그램 승인 흐름에 붙이기

> 이 문서를 GPT에게 그대로 전달하세요. v1(`작업지시서-카드뉴스연동.md`)을 대체합니다.
> 2026-08-29 기준. 바뀐 점: 카드 완성 대기와 세 번째 승인, 인스타그램 발행 호출이 추가됐고, `client_payload` 형식이 바뀌었습니다.

---

## 1. 배경

블로그 자동화는 이미 이렇게 돌고 있다.

```
07:00 소재 발굴 → 텔레그램 승인 ① → 글 작성 → 텔레그램 승인 ② → 워드프레스 게시
```

카드뉴스 이미지 생성과 인스타그램 발행은 **GitHub Actions에 이미 구축되어 동작이 확인된 상태**다.
앱이 할 일은 이 두 자동화를 잇는 것뿐이다. 새 서비스를 만들 필요는 없고, 지금 텔레그램·워드프레스를 부르는 것과 **같은 모양의 HTTP 호출 두 번**과 **대기 한 번**을 추가하면 된다.

완성되면 이렇게 된다.

```
07:00 소재 발굴 → 승인 ① → 글 작성 → 승인 ② → 워드프레스 게시
                                              ↓
                              [신규] 카드 JSON을 GitHub로 전송
                                              ↓
                              [신규] 카드 이미지 완성까지 대기 (약 2분)
                                              ↓
                              [신규] 텔레그램으로 카드 사진 + 캡션 → 승인 ③
                                              ↓
                              [신규] 인스타그램 발행 호출
```

승인 ③이 사람의 검수를 대신한다. 브라우저를 열지 않고 텔레그램에서 카드 그림을 보고 판단한다.

---

## 2. 공통 설정

| 항목 | 값 |
|---|---|
| 저장소 | `ttoottoo74-hub/silsolife_cardnews` |
| 토큰 | 환경변수 `GITHUB_CARDNEWS_TOKEN` (이미 앱에 등록됨). **소스코드에 하드코딩 금지** |
| 카드 공개 주소 | `https://ttoottoo74-hub.github.io/silsolife_cardnews` |

두 번의 GitHub 호출은 헤더가 같다.

```http
POST https://api.github.com/repos/ttoottoo74-hub/silsolife_cardnews/dispatches
Authorization: Bearer {GITHUB_CARDNEWS_TOKEN}
Accept: application/vnd.github+json
Content-Type: application/json
```

**성공은 HTTP 204이고 응답 본문이 비어 있다.** 본문이 없다고 실패로 처리하지 말 것.

| 응답 | 뜻 |
|---|---|
| 204 | 성공 |
| 401 / 403 | 토큰이 잘못됐거나 만료 |
| 404 | 토큰 권한 부족 또는 저장소 경로 오류 (GitHub는 권한 부족을 404로 감춘다) |
| 422 | `client_payload` 형식 오류 — 아래 3-1의 주의사항을 볼 것 |

---

## 3. 새로 넣을 세 가지

### 3-1. 워드프레스 게시 직후 — 카드 JSON 전송

```json
{
  "event_type": "cardnews",
  "client_payload": {
    "card": { "...카드 JSON 전체..." }
  }
}
```

> ⚠️ **`card` 한 겹으로 반드시 감쌀 것.**
> GitHub는 `client_payload`의 **최상위 항목을 10개까지만** 받는다. 카드 JSON은 최상위 항목이 16개라 그대로 넣으면 422로 거부된다. `card` 안에 통째로 넣으면 최상위 항목이 하나뿐이라 통과한다.
> 또한 `card` 값은 **JSON 객체 그대로** 넣는다. 문자열로 직렬화해서 넣지 않는다.

- 같은 글로 **하루에 한 번만** 호출한다. DB에 `cardnews_sent_at` 플래그를 둔다.
- **전송에 실패해도 블로그 발행과 DB 저장을 롤백하지 않는다.** 카드뉴스는 부가 산출물이다. 실패는 로그에 남기고 재시도 버튼을 제공한다.
- 전송에 성공하면 즉시 GitHub에서 카드 생성이 시작된다. 약 50초 걸린다.

### 3-2. 카드 완성 대기 → 텔레그램으로 승인 ③ 요청

토큰이 필요 없다. 공개 주소를 확인하기만 하면 된다.

```
BASE = https://ttoottoo74-hub.github.io/silsolife_cardnews/cards/{slug}
```

1. `{BASE}/card.json` 을 **15초 간격으로 확인**한다. 200이 되면 카드가 전부 준비된 것이다.
   - 최대 **5분**까지만 기다리고 포기한다. 포기하면 텔레그램으로 "카드 생성이 지연됩니다" 알림을 보내고 재시도 버튼을 남긴다.
   - GitHub Pages 배포까지 포함해 보통 100~130초 걸린다.
   - 캐시 때문에 404가 남아 있을 수 있으므로 주소 끝에 `?t={현재시각}` 을 붙여 확인한다.
2. `{BASE}/01.png` 부터 `{BASE}/10.png` 까지 순서대로 확인해 **200인 것만 모은다.**
   - 장수는 글마다 다르다(보통 8~10장). 10장으로 고정하지 말 것.
3. 모은 이미지를 **텔레그램 앨범(sendMediaGroup)** 으로 보내고, `card.json`의 `caption`과 `hashtags`도 함께 보낸다.
4. 승인 ①·②와 같은 방식으로 **승인 ③**을 요청한다. 버튼은 `발행` / `보류` 두 개면 충분하다.

> 이 대기 단계는 최대 5분이 걸린다. 앱의 작업이 짧은 수명의 함수(서버리스 등)에서 돌고 있다면 5분을 붙잡고 있지 말고, **별도의 후속 작업으로 분리**해서 큐나 스케줄러로 돌린다. 텔레그램 승인 콜백을 이미 처리하고 있으므로 그 구조를 그대로 쓰면 된다.

### 3-3. 승인 ③을 받으면 — 인스타그램 발행 호출

```json
{
  "event_type": "publish-instagram",
  "client_payload": { "slug": "{slug}" }
}
```

- 이것도 성공은 204다.
- `slug`는 영문·숫자·붙임표만 허용된다. 다른 문자가 섞이면 워크플로가 거부한다.
- 실제로 발행된다. 모의 실행으로 먼저 시험하고 싶으면 `client_payload`에 `"dry_run": true` 를 추가한다.
- 발행에는 30초쯤 걸린다. 결과를 확인하고 싶으면 인스타그램 계정을 보면 된다.
- `보류`를 누르면 아무것도 하지 않는다. 카드 이미지는 이미 만들어져 있으므로, 나중에 같은 `slug`로 다시 호출하면 그대로 발행된다.

---

## 4. 카드 JSON 스키마

`client_payload.card`에 아래 구조를 그대로 넣는다.

```json
{
  "version": "1.1",
  "published_at": "2026-08-28T13:00:32+09:00",
  "slug": "insurance-claim-deadline-filing-korea",
  "category": "돈·절약",
  "category_id": 3,
  "topic_label": "보험금 청구",
  "hook": "3년 지나면 못 받습니다",
  "title": "실손·자동차보험, 보험금은 언제까지 청구하나요?",
  "answer": "청구권 소멸시효는 3년입니다. 실손24로 온라인 청구가 가능합니다.",
  "visual": {
    "type": "timeline",
    "title": "3년은 언제부터 세나요?",
    "steps": [
      { "label": "사고·진료", "note": "기산일" },
      { "label": "알게 된 때", "note": "예외" },
      { "label": "3년", "note": "소멸시효" }
    ],
    "note": "특별한 사정이 없으면 보험사고가 발생한 때부터 진행합니다."
  },
  "cards": [
    {
      "heading": "기본 기한",
      "body": "보험금 청구권은 지급사유가 생긴 날부터 3년간 행사하지 않으면 시효로 소멸합니다.",
      "highlight": "3년"
    }
  ],
  "caution": "약관·상품별 예외가 있으니 내 약관과 감독기관 고시로 반드시 확인하세요.",
  "sources": [
    { "name": "국가법령정보센터", "url": "https://www.law.go.kr/..." }
  ],
  "verified_on": "2026-08-21",
  "caption": "실손보험금, 3년 지나면 청구가 막힙니다.\n\n지급사유가 생긴 날부터 3년입니다.\n\n자세한 내용은 프로필 링크에서 확인하실 수 있습니다.",
  "hashtags": ["실손보험", "보험금청구", "실손24", "소멸시효", "생활정보", "절약팁", "금융", "알아두면좋은정보"]
}
```

### 필드 규칙

| 필드 | 필수 | 제한 | 설명 |
|---|---|---|---|
| `version` | ○ | 고정 `"1.1"` | |
| `published_at` | ○ | ISO 8601 (+09:00) | 실제 발행 시각 |
| `slug` | ○ | 영문·숫자·붙임표 | 워드프레스에 발행된 slug와 완전히 동일 |
| `category` | ○ | 아래 5개 중 하나 | 가운뎃점 `·` 포함해 정확히 |
| `category_id` | ○ | 3~7 | 아래 표 참조. **카드 색이 이 값으로 갈린다** |
| `topic_label` | ○ | **8자 이내** | 표지 라벨. `보험금청구기한`보다 `보험금 청구`처럼 띄어 쓴다 |
| `hook` | ○ | **20자 이내** | 표지 큰 문구. **핵심 숫자를 반드시 넣는다** (아래 5번 참조) |
| `title` | ○ | 40자 이내 | 카드용으로 줄인 제목 |
| `answer` | ○ | **60자 이내** | 결론 한 문장 |
| `visual` | △ | 아래 참조 | 없으면 생략 |
| `cards` | ○ | **4~6개** | 본문 카드 |
| `cards[].heading` | ○ | **14자 이내** | |
| `cards[].body` | ○ | **75자 이내** | |
| `cards[].highlight` | ○ | 12자 이내 또는 `null` | 아래 6번 참조 |
| `caution` | ○ | 70자 이내 | 예외·주의점 하나 |
| `sources` | ○ | 1~3개 | `name`은 **기관명만 12자 이내** |
| `verified_on` | ○ | YYYY-MM-DD | |
| `caption` | ○ | **300자 이내** | 줄바꿈은 `\n` |
| `hashtags` | ○ | **8~15개** | `#` 없이, 공백 없이 |

### 카테고리 매핑

| 카테고리 | `category_id` | 카드 색 |
|---|---|---|
| 돈·절약 | 3 | 숲초록 |
| 정부·제도 | 4 | 감청 |
| 건강생활 | 5 | 청록 |
| 생활 | 6 | 테라코타 |
| 가족·노후 | 7 | 플럼 |

pillar 값을 이 표로 변환해서 넣는다. **`1`(Uncategorized)은 절대 쓰지 않는다.** 값이 비면 카드가 전부 초록으로 나온다.

---

## 5. hook 규칙 (카드 완성도를 가장 크게 좌우한다)

표지에서 가장 큰 글씨는 **`hook` 안의 첫 숫자 덩어리**다. 렌더러가 자동으로 찾아 1.34배로 키운다.

- 좋은 예 — `3년 지나면 못 받습니다` / `최대 70만원 지원` / `만 55세부터 가능` / `배출수수료 0원`
- 나쁜 예 — `놓치면 받을 돈이 사라집니다` (숫자가 없어 표지가 밋밋해진다)

숫자·금액·나이·기한 중 하나는 반드시 `hook`에 들어가야 한다.

---

## 6. highlight 규칙

`highlight`는 본문 카드에서 연녹색 상자에 담겨 크게 표시된다.

- `body`를 **먼저 자연스럽게 완성한 뒤**, 그 문장 안에 **이미 있는 표현을 그대로 잘라내** 넣는다.
- `highlight`를 만들려고 `body`에 단어나 괄호를 **덧붙이지 않는다.** `body` 끝에 `(3년 기준)` 같은 걸 붙이면 카드에 괄호가 그대로 찍힌다.
- **숫자 또는 고유명사 한 덩어리**를 우선한다.
  - 좋은 예: `3년`, `실손24`, `247만원`, `만 65세`, `정부보장사업`
  - 나쁜 예: `시효 시작`, `중복청구 금지`, `실손24 이용`
- 강조할 것이 없으면 `null`. 다만 `body`에 숫자가 있는데 `null`로 두지 않는다. **절반 이상의 카드는 highlight가 채워져야 한다.**

---

## 7. visual 블록 (선택)

숫자로 비교되거나 순서가 있는 정보가 있으면 넣는다. 없으면 **생략한다.**

> ⚠️ **본문 카드가 6개면 `visual`을 넣지 않는다.** 전체 장수가 인스타그램 캐러셀 상한 10장을 넘긴다.
> (표지 + 답 + 도식 + 본문 + 주의 + 출처)

**`timeline`** — 기한·시효
```json
{ "type": "timeline", "title": "3년은 언제부터 세나요?",
  "steps": [{"label":"사고·진료","note":"기산일"}, {"label":"3년","note":"소멸시효"}],
  "note": "" }
```
`steps` 3~4개, `label` 8자 이내, `note` 6자 이내.

**`compare`** — 금액 비교
```json
{ "type": "compare", "title": "2026년 선정기준액",
  "items": [{"label":"단독가구","value":247,"display":"월 247만원"}],
  "note": "" }
```
`value`는 막대 길이 계산용 **순수 숫자**(단위 없이), `display`는 표기용. `items` 2~4개.

**`steps`** — 신청 절차
```json
{ "type": "steps", "title": "청구는 이 순서로",
  "items": ["보험사에 사고 접수", "실손24로 서류 전송"], "note": "" }
```
`items` 3~4개, 각 16자 이내.

**`range`** — 자격 구간
```json
{ "type": "range", "title": "소득인정액 기준선",
  "low_label": "지원 대상", "high_label": "대상 아님",
  "cut": 0.62, "cut_label": "월 247만원", "note": "" }
```

공통: `title` 20자 이내, `note` 60자 이내(없으면 `""`).

---

## 8. 문구 작성 규칙

- 카드 문구는 **요약이 아니라 카드 한 장 분량**이다. 본문을 잘라 붙이지 말고 새로 쓴다.
- 금액·기한·나이·비율이 있으면 살린다. 숫자 없는 카드가 절반을 넘지 않게 한다.
- 문체는 블로그와 동일하게 존댓말·평서문. 과장·낚시성 표현은 쓰지 않는다.
- `sources`는 **1차 출처**만. 뉴스 기사나 블로그는 쓰지 않는다. `url`은 **그 문장의 근거가 되는 바로 그 문서**를 가리켜야 하며, 기관 홈페이지로 대충 채우지 않는다.
- `caption`은 인스타그램용이다. `[결론 한 문장]` → 빈 줄 → `[핵심 근거]` → 빈 줄 → `자세한 내용은 프로필 링크에서 확인하실 수 있습니다.` 구조로 쓰고, 공문 말투(`~하십시오`)는 쓰지 않는다.

---

## 9. 설정 화면에 추가할 것

- **"카드뉴스 전송 테스트"** 버튼 — 최근 콘텐츠 하나로 3-1을 시도하고 결과(204/오류 코드)를 보여준다
- 콘텐츠 상세 화면에 **"카드뉴스 다시 보내기"** 버튼
- 콘텐츠별 상태 표시: `미전송` / `전송됨` / `카드 완성` / `승인 대기` / `발행됨` / `실패`
- 승인 ③에서 `보류`를 누른 건은 나중에 다시 발행할 수 있도록 목록에 남긴다

---

## 10. 앱이 하지 않아도 되는 것

- 카드 이미지 생성 — GitHub Actions가 한다
- 인스타그램 API 호출 — GitHub Actions가 한다 (앱은 발행 신호만 보낸다)
- 글자 수 초과·형식 오류 교정 — 파이프라인에 자동 교정기와 검증기가 있다
- 인스타그램 토큰 관리 — GitHub에 등록되어 있다

앱은 **정확한 JSON을 한 번 보내고, 완성을 기다렸다가, 승인을 받아 발행 신호를 한 번 더 보내는 것**까지만 하면 된다.

---

## 11. 확인 방법

- 실행 기록: https://github.com/ttoottoo74-hub/silsolife_cardnews/actions
- 완성된 카드: https://ttoottoo74-hub.github.io/silsolife_cardnews
- 인스타그램: https://www.instagram.com/silsolife/

3-1을 보내면 `카드뉴스 생성`에 실행이 뜨고, 3-3을 보내면 `인스타그램 발행`에 실행이 뜬다. 둘 다 초록 체크면 성공이다.
